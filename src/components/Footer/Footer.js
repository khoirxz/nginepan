import { Text } from "@chakra-ui/react";
import React from "react";
import ContainerLayout from "../../utils/ContainerLayout";

const Footer = () => {
  return (
    <ContainerLayout>
      <div>
        <Text>Footer components</Text>
      </div>
    </ContainerLayout>
  );
};

export default Footer;
